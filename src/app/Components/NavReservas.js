expor;
export default function NaveReservas() {
  return (

    // Funcionalidades:
    //     Filtros: Fecha, externo, evento
    // Externo filtro para mostrar tabla
    // Tabla: fecha, hora, evento, ambiente
    <nav>
      <div>

      </div>
    </nav>
  );
}
